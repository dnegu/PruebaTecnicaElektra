<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PruebaTécnicaElektra" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PruebaTécnicaElektra" parent="Base.Theme.PruebaTécnicaElektra" />

    <style name="Theme.Test.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Test.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="WhiteAccent">
        <item name="colorAccent">@color/white</item> <!-- Whatever color you want-->
    </style>

    <declare-styleable name="CircularImageView">
        <attr name="initialsBackgroundColor" format="color" />
        <attr name="initialsTextColor" format="color" />
        <attr name="placeholderDrawable" format="reference" />
        <attr name="initialsTextSize" format="dimension" />
    </declare-styleable>
</resources>